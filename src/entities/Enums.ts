export enum Accessibility {
  PUBLIC,
  PRIVATE,
  INACCESSIBLE
}

export enum Material {
  WOOD,
  METAL,
  PLASTIC,
  GLASS,
  OTHER
}

export enum Size {
  SMALL,
  MEDIUM,
  LARGE
}

export enum Type {
  OBITUARY,
  POLITICS,
  NOTICE,
  PUBLIC,
  OTHER
}
